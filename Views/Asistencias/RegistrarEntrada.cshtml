@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<h2>Registrar Entrada (HU0403)</h2>

<form asp-action="RegistrarEntrada" method="post">
    <div class="mb-3">
        <label>Seleccione Empleado</label>
        <select name="IdEmpleado" class="form-select" asp-items="ViewBag.Empleados" required>
            <option value="">Seleccione...</option>
        </select>
    </div>

    <div class="alert alert-info">
        <strong>Hora actual:</strong> @DateTime.Now.ToString("HH:mm:ss")
    </div>

    <button class="btn btn-success">Registrar Entrada</button>
    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
</form>